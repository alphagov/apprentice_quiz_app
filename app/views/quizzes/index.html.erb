<h1>All Quizzes</h1>
<%= render "govuk_publishing_components/components/button", text: "Create New Quiz", href: new_quiz_path %>


<ul>
  <% @quizzes.each do |quiz| %>
    <li><%= link_to quiz.title, quiz_path(quiz) %></li>
  <% end %>
</ul>
